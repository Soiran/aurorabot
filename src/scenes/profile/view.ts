import Scene from '../../scene';
import { bot } from '../../index';
import { Keyboard } from 'vk-io';
import { Profile } from '../../types';


export default class ProfileViewScene extends Scene {
    constructor(profile?: Profile) {
        super(
            async (scene) => {
                
            },
            [
                
            ]
        );
    }
}